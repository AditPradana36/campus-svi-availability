# Geospatial
geopandas>=0.14
shapely>=2.0
pyogrio>=0.7
pyproj>=3.5
numpy>=1.24
pandas>=2.0

# Mapillary Graph API v4
aiohttp>=3.9

# Google Street View metadata. Wraps Google's internal endpoints: no API key,
# no billing. Those endpoints are undocumented and can change without notice,
# so pin the version you publish results with.
streetlevel>=0.12

# Lets the async fetchers run inside Colab/Jupyter
nest-asyncio>=1.6

# Figures
matplotlib>=3.7

# Satellite basemap for the study-area panels. Optional: without it those
# panels fall back to a plain fill and nothing else is affected.
contextily>=1.5
